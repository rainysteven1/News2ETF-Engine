# LLM Provider Credentials
# Copy this file to llm_credentials.yaml and fill in your actual API keys.
# The llm_credentials.yaml file is gitignored - never commit real credentials.

zhipu:
  api_key: "your-zhipu-api-key"
  base_url: "https://open.bigmodel.cn/api/paas/v4"

minimax:
  api_key: "your-minimax-api-key"
  base_url: "https://api.minimax.chat/v1"
