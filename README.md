# News2ETF-Engine

A Python engine for classifying news articles into ETF categories, using a SQLite database and Polars for data manipulation. The engine provides a command-line interface (CLI) for processing news data and updating classifications efficiently.
